@tailwind base;
@tailwind components;
@tailwind utilities;

html {
  scroll-behavior: smooth;
}

section[id] {
  scroll-margin-top: 90px;
}

.dystopic-root {
  --c-void: #090909;
  --c-abyss: #101714;
  --c-forest: #1B352A;
  --c-accent: #4F8A6B;
  --c-cream: #EDEAE3;
  --c-stone: #B7C0BA;
  --c-stone-dim: #7C8985;
  background-color: var(--c-void);
  color: var(--c-cream);
  font-family: 'Inter', sans-serif;
  overflow-x: hidden;
}

.dystopic-root * {
  box-sizing: border-box;
}

.font-heading { font-family: 'Cinzel', serif; font-weight: 600; }
.font-subheading { font-family: 'Cormorant Garamond', serif; }
.font-body { font-family: 'Inter', sans-serif; }

.wordmark {
  font-family: 'Cinzel', serif;
  font-weight: 500;
  color: var(--c-cream);
  letter-spacing: 0.15em;
  background: none;
  border: none;
  cursor: pointer;
}

.accent-text { color: var(--c-accent); }
.text-stone { color: var(--c-stone); }
.text-stone-dim { color: var(--c-stone-dim); }

.eyebrow {
  font-family: 'Inter', sans-serif;
  font-size: 0.75rem;
  letter-spacing: 0.35em;
  text-transform: uppercase;
  color: var(--c-accent);
}

.section-void { background-color: var(--c-void); }
.section-abyss { background-color: var(--c-abyss); }

.hero-bg {
  background: radial-gradient(ellipse at 50% 20%, #14201b 0%, var(--c-void) 65%);
}

.hero-blob {
  position: absolute;
  border-radius: 9999px;
  filter: blur(90px);
  opacity: 0.28;
  pointer-events: none;
}
.hero-blob-1 {
  width: 480px;
  height: 480px;
  background: var(--c-forest);
  top: -10%;
  left: -10%;
  animation: blobDrift1 22s ease-in-out infinite;
}
.hero-blob-2 {
  width: 400px;
  height: 400px;
  background: var(--c-accent);
  bottom: -15%;
  right: -5%;
  opacity: 0.16;
  animation: blobDrift2 26s ease-in-out infinite;
}
.hero-blob-3 {
  width: 420px;
  height: 420px;
  background: var(--c-forest);
  top: 10%;
  left: 50%;
  transform: translateX(-50%);
  opacity: 0.2;
}

@keyframes blobDrift1 {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50% { transform: translate(60px, 40px) scale(1.1); }
}
@keyframes blobDrift2 {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50% { transform: translate(-50px, -30px) scale(1.15); }
}

.logo-ring {
  width: 64px;
  height: 64px;
  border: 1px solid rgba(79, 138, 107, 0.4);
  border-radius: 9999px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(27, 53, 42, 0.3);
}

.hero-heading { color: var(--c-cream); font-weight: 700; }

.nav-scrolled {
  background: rgba(9, 9, 9, 0.75);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  border-bottom: 1px solid rgba(79, 138, 107, 0.15);
}

.nav-link {
  color: var(--c-stone);
  background: none;
  border: none;
  cursor: pointer;
  position: relative;
  transition: color 0.3s ease;
}
.nav-link::after {
  content: '';
  position: absolute;
  left: 0;
  bottom: -4px;
  width: 0%;
  height: 1px;
  background: var(--c-accent);
  transition: width 0.3s ease;
}
.nav-link:hover { color: var(--c-cream); }
.nav-link:hover::after { width: 100%; }

.mobile-menu {
  background: rgba(9, 9, 9, 0.97);
  backdrop-filter: blur(14px);
  border-bottom: 1px solid rgba(79, 138, 107, 0.15);
}

.btn-primary {
  font-family: 'Inter', sans-serif;
  font-size: 0.875rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  padding: 1rem 2.25rem;
  background: var(--c-accent);
  color: var(--c-void);
  border: 1px solid var(--c-accent);
  border-radius: 2px;
  cursor: pointer;
  transition: all 0.35s ease;
}
.btn-primary:hover {
  background: transparent;
  color: var(--c-cream);
  box-shadow: 0 0 24px rgba(79, 138, 107, 0.35);
}

.btn-secondary {
  font-family: 'Inter', sans-serif;
  font-size: 0.875rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  padding: 1rem 2.25rem;
  background: transparent;
  color: var(--c-cream);
  border: 1px solid rgba(237, 234, 227, 0.3);
  border-radius: 2px;
  cursor: pointer;
  transition: all 0.35s ease;
}
.btn-secondary:hover {
  border-color: var(--c-accent);
  box-shadow: 0 0 24px rgba(79, 138, 107, 0.25);
  color: var(--c-accent);
}

.scroll-cue {
  background: none;
  border: none;
  cursor: pointer;
  animation: bounce 2.4s ease-in-out infinite;
}
@keyframes bounce {
  0%, 100% { transform: translate(-50%, 0); opacity: 0.6; }
  50% { transform: translate(-50%, 10px); opacity: 1; }
}

.reveal {
  opacity: 0;
  transform: translateY(28px);
  transition: opacity 0.9s cubic-bezier(0.16, 1, 0.3, 1), transform 0.9s cubic-bezier(0.16, 1, 0.3, 1);
}
.reveal-visible {
  opacity: 1;
  transform: translateY(0);
}

.placeholder-block {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, rgba(27,53,42,0.9), rgba(16,23,20,0.9));
  color: var(--c-accent);
  border: 1px solid rgba(79, 138, 107, 0.2);
  overflow: hidden;
}

.service-card {
  background: rgba(16, 23, 20, 0.6);
  border: 1px solid rgba(79, 138, 107, 0.15);
  border-radius: 8px;
  padding: 2.5rem 2rem;
  transition: transform 0.4s ease, border-color 0.4s ease, box-shadow 0.4s ease;
}
.service-card:hover {
  transform: translateY(-6px);
  border-color: rgba(79, 138, 107, 0.45);
  box-shadow: 0 20px 40px -20px rgba(79, 138, 107, 0.25);
}
.service-icon {
  width: 56px;
  height: 56px;
  border-radius: 9999px;
  border: 1px solid rgba(79, 138, 107, 0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--c-accent);
  background: rgba(79, 138, 107, 0.06);
}
.dot {
  width: 4px;
  height: 4px;
  border-radius: 9999px;
  background: var(--c-accent);
  display: inline-block;
  flex-shrink: 0;
}

.project-card {
  text-decoration: none;
  background: none;
  border: none;
  padding: 0;
  cursor: pointer;
  width: 100%;
}
.project-image {
  position: relative;
  aspect-ratio: 16 / 10;
  border-radius: 6px;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, rgba(27,53,42,0.9), rgba(16,23,20,0.9));
  transition: transform 0.5s ease;
}
.project-card:hover .project-image { transform: scale(1.02); }
.project-thumb-img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: brightness(0.6);
  transition: filter 0.3s ease;
}
.project-card:hover .project-thumb-img { filter: brightness(0.48); }

.play-button {
  position: relative;
  z-index: 1;
  width: 56px;
  height: 56px;
  border-radius: 9999px;
  border: 1px solid rgba(79, 138, 107, 0.6);
  background: rgba(9, 9, 9, 0.55);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--c-cream);
  transition: all 0.35s ease;
}
.project-card:hover .play-button {
  background: var(--c-accent);
  color: var(--c-void);
  box-shadow: 0 0 24px rgba(79, 138, 107, 0.45);
  transform: scale(1.08);
}

.video-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(9, 9, 9, 0.88);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  padding: 1.5rem;
}
.video-modal-frame {
  position: relative;
  width: 100%;
  max-width: 960px;
  aspect-ratio: 16 / 9;
  background: var(--c-void);
  border-radius: 10px;
  overflow: hidden;
  border: 1px solid rgba(79, 138, 107, 0.3);
  box-shadow: 0 30px 80px -20px rgba(0, 0, 0, 0.6);
}
.video-modal-close {
  position: absolute;
  top: 12px;
  right: 12px;
  z-index: 2;
  width: 36px;
  height: 36px;
  border-radius: 9999px;
  border: 1px solid rgba(79, 138, 107, 0.4);
  background: rgba(9, 9, 9, 0.65);
  color: var(--c-cream);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.3s ease;
}
.video-modal-close:hover { border-color: var(--c-accent); color: var(--c-accent); }

.link-arrow { transition: transform 0.3s ease; display: inline-block; }
.project-card:hover .link-arrow { transform: translate(3px, -3px); }

.why-icon {
  width: 52px;
  height: 52px;
  border-radius: 9999px;
  border: 1px solid rgba(79, 138, 107, 0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--c-accent);
}

.social-btn {
  width: 88px;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 1.25rem 0.5rem;
  border: 1px solid rgba(79, 138, 107, 0.2);
  border-radius: 8px;
  color: var(--c-stone);
  text-decoration: none;
  transition: all 0.35s ease;
}
.social-btn:hover {
  border-color: var(--c-accent);
  color: var(--c-accent);
  transform: translateY(-4px);
  box-shadow: 0 12px 24px -12px rgba(79, 138, 107, 0.4);
}

.border-t-hairline { border-top: 1px solid rgba(79, 138, 107, 0.15); }

@media (max-width: 768px) {
  .hero-heading { line-height: 1.15; }
}

@media (prefers-reduced-motion: reduce) {
  .reveal { transition: none; }
  .hero-blob, .scroll-cue { animation: none; }
}
